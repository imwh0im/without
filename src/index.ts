import { ContentType, RedirectionDepthType, SpamLinkDomainsType } from "./type";

async function isSpam(
  content: ContentType,
  spamLinkDomains: SpamLinkDomainsType,
  redirectionDepth: RedirectionDepthType,
): Promise<boolean> {
  

  return true;
}
